# Chef generated config
# API entry point
jenkins: 'http://qa-s2:8080'
repos:
  # pull requests to dev
  - repo: Wikia/sparrow
    events:
    - pull_request
    - pull_request_review_comment
    mentions:
    - '@WikiaSparrow'
    jobs:
    - sparrow_runner